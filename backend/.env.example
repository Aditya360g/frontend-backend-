# Reference only. Spring Boot does not load this file automatically.
# Set these values as environment variables before running the backend.
DB_USERNAME=root
DB_PASSWORD=change_me
DB_URL=jdbc:mysql://127.0.0.1:3306/selfb_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
JWT_SECRET=replace_with_a_base64_encoded_secret
CORS_ALLOWED_ORIGINS=http://localhost:5173
REFRESH_TOKEN_COOKIE_SECURE=false
PASSWORD_RESET_URL=http://localhost:5173/reset-password
